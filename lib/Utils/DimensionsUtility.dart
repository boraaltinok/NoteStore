import 'package:get/get.dart';
class DimensionsUtility {

  static double screenHeight = Get.context!.height;//2400
  static double screenWidth = Get.context!.width;//1080

  static double drawerWidth = screenWidth / 1.3;
  static double drawerHeight = screenHeight;

  static double drawerWidthHalf = drawerWidth / 2;

  static double drawerProfileHeight = drawerHeight / 4;

  static double drawerProfileHeightHalf = drawerProfileHeight / 2;

  static double drawerHeightHalf = drawerHeight / 2;
  static double drawerHeightQuarter = drawerHeight / 4;



}